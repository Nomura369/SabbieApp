import { Center, Text } from "@gluestack-ui/themed";

const StatisticsScreen = () => {
    return (
        <Center flex={1}>
            <Text>星情統計圖表（施工中）</Text>
        </Center>
    );
}

export default StatisticsScreen;