import { Center, Text } from "@gluestack-ui/themed";

const CompletionScreen = () => {
    return (
        <Center flex={1}>
            <Text>星砂瓶完成頁面（施工中）</Text>
        </Center>
    );
}

export default CompletionScreen;